# 업무망 Grafana API 기반 AI LLM 운영지원 에이전트 개발 계획서

---

## 1. 추진 배경

현재 업무망에서는 보안 정책상 Prometheus, Loki 등 원천 모니터링 서버에 직접 접근이 제한된다.
다만 업무 PC에서는 Grafana 로그인을 통해 서버 메트릭 및 로그를 조회할 수 있다.

이에 따라 Grafana API를 활용하여 업무 PC에서 데이터를 수집하고, Python 기반 에이전트를 통해 가공 후 로컬 LLM으로 분석·요약하는 운영지원 도구를 개발한다.

---

## 2. 개발 목표

업무 PC 환경에서 실행 가능한 경량 AI 에이전트를 구축한다.

### 주요 목표

* Grafana API 기반 데이터 수집 자동화
* 로그 및 메트릭 데이터 정규화 및 요약
* 로컬 LLM 기반 장애 분석 및 설명 생성
* CLI 기반 사용자 인터페이스 제공
* 운영자용 리포트 자동 생성
* EXE 배포 및 메일/메신저 연동 확장 가능 구조 확보

---

## 3. 개발 범위

### 3.1 1차 개발 범위

* Grafana API 토큰 인증
* 대시보드/패널 데이터 조회
* 서버 메트릭 수집 (CPU, Memory 등)
* 거래 로그 및 에러 로그 수집
* JSON 데이터 전처리
* LLM 입력용 요약 데이터 생성
* llama.cpp API 연동 (Qwen3-0.6B-Q4_K_M.gguf)
* CLI 기반 명령어 처리
* 분석 결과 CLI 출력
* 텍스트 파일 저장 기능
* 리포트 생성 (TXT/Markdown)

### 3.2 후속 개발 범위

* SMTP 이메일 전송
* 업무망 메신저 연동
* Windows EXE 패키징
* 스케줄러 자동 실행
* 프롬프트 고도화

---

## 4. 전체 구성도

```
[CLI 사용자 입력]
        |
        v
[Python Agent]
        |
        v
[Grafana API]
        |
        v
[데이터 수집 (JSON)]
        |
        v
[전처리 / 집계]
        |
        v
[llama.cpp + Qwen3-0.6B]
        |
        v
[결과 생성]
        |
        v
[CLI 출력 / 파일 저장 / 메일 / 메신저]
```

---

## 5. 주요 기능

### 5.1 데이터 수집

* Grafana API 토큰 기반 인증
* 메트릭 및 로그 데이터 조회

| 구분  | 항목                |
| --- | ----------------- |
| 서버  | CPU, Memory, Disk |
| WAS | Thread, Heap, GC  |
| 로그  | ERROR, WARN       |
| 거래  | 승인/실패 로그          |
| 트렌드 | 일별 평균, 증감률        |

---

## 6. 데이터 처리 방식

```
원본 JSON
→ 필드 추출
→ 로그 그룹핑
→ 통계 집계
→ 요약 데이터 생성
→ LLM 입력
```

### 예시

```json
{
  "server": "app01",
  "cpu_avg": "75%",
  "error_count": 32,
  "top_error": "DB timeout"
}
```

---

## 7. LLM 활용 방식

LLM은 원본 분석이 아닌 요약 기반 해석 역할 수행

* 상태 요약
* 장애 원인 후보
* 점검 항목
* 조치 우선순위

---

## 8. 기술 스택

| 구분     | 기술               |
| ------ | ---------------- |
| 언어     | Python           |
| API    | requests         |
| 데이터 처리 | pandas           |
| LLM    | llama.cpp (Qwen3-0.6B-Q4_K_M.gguf) |
| CLI    | argparse / click |
| 설정     | YAML             |
| 리포트    | Markdown / TXT   |
| 패키징    | PyInstaller      |
| 환경     | Windows          |

---

## 9. 프로젝트 구조

```
grafana-ai-agent/
 ├─ main.py
 ├─ config.yaml
 ├─ src/
 │   ├─ grafana_client.py
 │   ├─ collector.py
 │   ├─ preprocessor.py
 │   ├─ llm_client.py
 │   ├─ reporter.py
 │   ├─ notifier.py
 ├─ logs/
 ├─ reports/
```

---

## 10. CLI 사용자 인터페이스

### 10.1 CLI 기반 대화

```bash
agent.exe analyze --server app01 --time 1h
```

출력:

```
상태 요약:
- CPU 92%
- ERROR 증가

원인:
- DB timeout 가능성
```

---

### 10.2 CLI 명령어

| 명령어     | 설명     |
| ------- | ------ |
| analyze | 서버 분석  |
| trend   | 트렌드 분석 |
| log     | 로그 조회  |
| report  | 리포트 생성 |

---

### 10.3 파일 저장 기능

```bash
agent.exe analyze --server app01 --output file
```

생성 파일:

```
report_app01_YYYYMMDD.txt
```

---

## 11. 개발 단계

| 단계 | 내용             |
| -- | -------------- |
| 1  | Grafana API 연동 |
| 2  | 데이터 수집         |
| 3  | 전처리            |
| 4  | LLM 연동         |
| 5  | CLI 구현         |
| 6  | 리포트 생성         |
| 7  | EXE 패키징        |
| 8  | 메일/메신저 연동      |

---

## 12. 보안 고려사항

* API 토큰 하드코딩 금지
* 민감정보 마스킹
* 내부망 저장
* LLM 결과는 참고용 사용

---

## 13. 기대 효과

* 운영 분석 시간 단축
* 장애 대응 속도 향상
* CLI 기반 간편 실행
* 리포트 자동 생성
* 향후 자동화 확장 가능

---

## 14. 리스크 및 대응

| 리스크    | 대응     |
| ------ | ------ |
| 데이터 과다 | 집계/샘플링 |
| LLM 느림 | 소형 모델  |
| 오답     | 참고용 활용 |

---

## 15. 결론

본 시스템은 Grafana API 기반으로 데이터를 수집하고 Python 에이전트를 통해 가공 후 로컬 LLM으로 분석하는 구조이다.
CLI 기반 인터페이스를 통해 사용자가 직접 요청하고 결과를 확인하며, 필요 시 파일로 저장하는 방식으로 운영 효율을 향상시킨다.

초기에는 리포트 중심으로 구축하고 이후 메일 및 메신저 연동으로 확장한다.
