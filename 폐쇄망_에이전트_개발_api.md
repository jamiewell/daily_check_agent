# 폐쇄망 환경을 고려한 AI 에이전트 개발 가이드

## 1. 개요

본 가이드는 폐쇄망(업무망) 환경에서 AI/LLM 기반 에이전트를 개발하고 활용하기 위한 표준 절차 및 고려사항을 정의한다.  
특히 외부 인터넷망과 내부 업무망이 분리된 환경에서 **보안 준수, 안정성 확보, 내부 LLM 활용**을 목표로 한다.

---

## 2. 기본 원칙

### 2.1 개발 원칙
- 개발은 **외부 인터넷망에서 수행**
- 완성된 결과물은 **내부망 반입 후 사용**
- 내부망에서는 **사내 LLM API만 사용**
- 외부 상용 LLM(OpenAI, Google 등)은 내부망 사용 불가

### 2.2 아키텍처 원칙
- 외부 선개발 → 내부 반입 → 내부 LLM 연동
- 내부 시스템 연동은 **API 기반만 허용**
- 소스코드 수정 없이 **환경 설정만으로 API 변경 가능**해야 함

---

## 3. 전체 프로세스

### Step 1. 에이전트 기획 및 정의

#### 3.1 목적 정의
- 해결하고자 하는 업무 문제 정의
- 자동화 대상 워크플로우 구체화

#### 3.2 기능 유형 결정
- 단순 LLM 활용 (요약, 분류, 생성)
- RAG 기반 검색형 에이전트
- OCR/문서 이해형
- 내부 시스템 연동형

#### 3.3 기술 스택 선정
- 외부 개발 도구 및 프레임워크 선정
- EXE 패키징 가능 여부 고려

---

### Step 2. 보안 및 컴플라이언스 사전 협의

#### 3.4 반입 적정성 검토
- 에이전트 목적 및 작동 원리 설명
- 내부망 반입 가능 여부 검토

#### 3.5 데이터 및 시스템 연계 검토
- 내부 데이터 접근 필요 여부
- 민감 데이터 처리 여부
- 로그 저장 범위 정의

#### 3.6 정보보호 승인
- EXE 파일 반입 절차 준수
- 방화벽 통신 허용 신청 필요

---

### Step 3. LLM 인터페이스 및 자원 협의

#### 3.7 모델 검증
- 사내 LLM 사용 가능 모델 확인
  - 예: Qwen3, Qwen3-VL, KURE-v1, bge-reranker

#### 3.8 API 규격 확보
- REST API 규격
- JSON 스키마
- 인증 방식(API Key)
- 호출 제한량

#### 3.9 개발환경 적용
- 외부 개발 시 Mock API 활용
- 내부 API와 동일한 인터페이스 유지

---

### Step 4. 외부망 개발 및 빌드

#### 3.10 개발 수행
- 외부 AI 도구 적극 활용 (ChatGPT, Claude, Cursor 등)
- 에이전트 로직 구현

#### 3.11 패키징
- PyInstaller 등을 활용한 EXE 생성
- 또는 Docker 이미지 패키징

#### 3.12 보안 준비
- 백신(V3) 검사 수행
- HASH 값 확보
- 사용 라이브러리 목록 정리

---

### Step 5. 내부망 반입 및 연동

#### 3.13 파일 반입
- 망간 자료 전송 절차 준수

#### 3.14 환경 설정
- 외부 API → 내부 LLM API 주소 변경
- 설정 파일 기반 구성

#### 3.15 검증 및 배포
- 내부망 테스트 수행
- 운영 배포

---

## 4. 권장 아키텍처
[사용자]
↓
[에이전트 EXE]
↓
[전처리]
↓
[사내 LLM API]
↓
[결과 후처리]
↓
[화면 출력 / 파일 저장]



### 주요 구성 요소
- UI 모듈
- 입력 처리 모듈
- 프롬프트 생성 모듈
- LLM API 클라이언트
- 결과 처리 모듈
- 설정 및 로그 관리

---

## 5. 기술 스택 권장안

### 5.1 기본 스택
- Language: Python
- UI: Tkinter / PySide6
- HTTP: requests
- Packaging: PyInstaller
- Logging: logging

### 5.2 데이터 처리
- pandas
- openpyxl
- python-docx

### 5.3 선택 사항 (RAG)
- FAISS (로컬 벡터DB)
- 사내 임베딩 모델 활용

---

## 6. 설계 시 필수 고려사항

### 6.1 보안
- 민감 데이터 저장 금지
- 로그에 원문 데이터 저장 제한
- API 호출 데이터 최소화

### 6.2 데이터
- 타 그룹사 데이터 저장 금지
- 벡터DB 저장 위치 명확화

### 6.3 네트워크
- 내부망 API만 호출
- 외부 인터넷 통신 금지

### 6.4 설정 분리
- API 주소
- API Key
- 모델명
- 로그 레벨

→ 반드시 설정파일로 분리

---

## 7. 제한사항

- 외부 LLM 서비스 사용 불가
- DRM 문서 활용 제한
- 내부 시스템 직접 DB 접근 제한
- 과도한 API 호출 제한 존재

---

## 8. 기대 효과

- 업무 자동화 및 생산성 향상
- 반복 업무 감소
- 장애 대응 속도 향상
- AI 활용 경험 확산

---

## 9. 추천 활용 사례

### 9.1 운영 로그 분석
- GC 로그 분석
- 장애 로그 요약

### 9.2 보고서 자동 생성
- 점검 결과 정리
- 장애 보고서 생성

### 9.3 업무 가이드 검색
- 표준 운영 절차 조회
- 장애 대응 매뉴얼 검색

---

## 10. 준비 체크리스트

- [ ] 과제 정의서 작성
- [ ] 데이터 흐름도 작성
- [ ] 보안 검토 항목 정리
- [ ] LLM API 요구사항 확인
- [ ] Mock API 개발 완료
- [ ] EXE 패키징 완료
- [ ] 백신 검사 및 HASH 확보
- [ ] 내부 반입 승인 완료

---

## 11. 결론

폐쇄망 환경에서의 AI 에이전트 개발은  
단순한 기술 구현이 아니라 **보안, 데이터, 아키텍처를 동시에 고려한 설계 작업**이다.

핵심은 다음과 같다.

> 외부에서 개발하고, 내부에서 안전하게 실행하며, 사내 LLM과 유연하게 연동되는 구조를 설계하는 것

---